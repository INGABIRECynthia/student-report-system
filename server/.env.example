PORT=5000
NODE_ENV=development

# Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=student_report_system

# JWT — REQUIRED: replace with a long, random string before deploying to production
JWT_SECRET=change-this-to-a-long-random-secret-value
JWT_EXPIRES_IN=7d

# CORS
CLIENT_URL=http://localhost:3000
